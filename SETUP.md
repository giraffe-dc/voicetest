# Voice Signal - Real-time Audio Frequency Analyzer

Сайт для визначення рівня голосу з синхронізацією виводу даних на різних пристроях. Запускається на телефонах та комп'ютерах з одночасним виводом еквалайзера в реальному часі.

## 🎯 Можливості

- **Real-time Audio Analysis** - Аналіз частотного спектру звуку в режимі реального часу
- **Multi-Device Sync** - Синхронізація даних між декількома підключеними пристроями через WebSocket
- **Adaptive Design** - Адаптивний дизайн:
  - **Мобільні пристрої**: Вертикальний еквалайзер
  - **Комп'ютери**: Горизонтальний еквалайзер
- **Modern UI** - Сучасний темний дизайн з анімаціями та градієнтами
- **Colorful Equalizer** - 32-смуговий еквалайзер з кольоровими падаючими кружечками
- **Room Connection** - Підключення до "кімнати" для спільного перегляду

## 🛠️ Технологічний стек

- **Frontend**: React 19, Next.js 16, TypeScript, Tailwind CSS 4
- **Backend**: Next.js API Routes, Socket.IO
- **Real-time Communication**: Socket.IO (WebSocket + Polling)
- **Audio API**: Web Audio API, getUserMedia

## 📦 Встановлення

### Вимоги
- Node.js 18+ 
- npm або yarn

### Кроки встановлення

1. **Встановити залежності**:
```bash
npm install
```

2. **Запустити development server**:
```bash
npm run dev
```

3. **Відкрити сайт**:
   - Основна адреса: http://localhost:3000
   - На мобільному пристрої: http://<ваша-IP>:3000

## 🚀 Використання

### Базовий функціонал

1. **Запуск прослуховування**:
   - Натисніть кнопку "🎤 Start Listening"
   - Дозволіть доступ до мікрофона

2. **Синхронізація на іншому пристрої**:
   - Відкрийте той же URL на іншому пристрої (телефон/комп'ютер)
   - Обидва пристрої отримають однакові дані еквалайзера

3. **Перегляд віддалених даних**:
   - Якщо з'єднаних декілька пристроїв, натисніть "👥 Remote Audio" для перегляду даних з іншого пристрою

## 📱 Адаптивність

### На мобільному пристрої
- Вертикальний еквалайзер (32 смуги)
- Компактний інтерфейс
- Великі сенсорні кнопки

### На комп'ютері
- Горизонтальний еквалайзер (32 смуги)
- Розширений інтерфейс
- Статус підключення

## 🎨 Дизайн

- **Колір**: Темна тема з фіолетовими і рожевими акцентами
- **Анімація**: Плавні переходи, пульсуючі індикатори
- **Градієнти**: Мультиколірні градієнти для еквалайзера (червоний → помаранчевий → жовтий → зелений → синій → пурпурний)

## 📊 Структура еквалайзера

- **Кількість смуг**: 32 частотних смуги
- **Діапазон**: 0-256 (нормалізовано)
- **Анімація**: Smooth 75ms transition
- **Ефекти**: 
  - Градієнтна заливка
  - Світлова аура (glow effect)
  - Пульсуючі кружечки на вершинах

## 🔗 WebSocket подія

### На клієнті (Client Events)
- `audio-data`: Відправка частотних даних на сервер
  ```javascript
  socket.emit('audio-data', {
    frequencies: Uint8Array[256],
    timestamp: number
  });
  ```

### На сервері (Server Events)
- `audio-update`: Отримання даних від іншого клієнта
  ```javascript
  io.emit('audio-update', {
    frequencies: number[],
    timestamp: number,
    clientId: string
  });
  ```

## 🏗️ Структура проекту

```
voicesignal/
├── app/
│   ├── components/
│   │   └── Equalizer.tsx          # Компонент еквалайзера
│   ├── hooks/
│   │   ├── useAudioAnalyzer.ts    # Хук для обробки аудіо
│   │   └── useSocketAudio.ts      # Хук для WebSocket
│   ├── globals.css                # Глобальні стилі
│   ├── layout.tsx                 # Root layout
│   └── page.tsx                   # Основна сторінка
├── pages/
│   └── api/
│       └── socket.ts              # Socket.IO обробник
├── package.json
├── next.config.ts
└── tsconfig.json
```

## ⚙️ Налаштування

### Змінення кількості смуг еквалайзера

Відредагуйте файл `app/components/Equalizer.tsx`:
```typescript
const barCount = 32; // Змініть на потрібне значення
```

### Змінення аналізатора

Відредагуйте файл `app/hooks/useAudioAnalyzer.ts`:
```typescript
analyzer.fftSize = 2048; // Розмір FFT
```

## 🔧 Відомі проблеми

1. **HTTPS потрібен для мікрофона** - На продакшене сайт повинен мати SSL сертифікат
2. **CORS** - При доступі через IP потрібно налаштувати CORS на сервері
3. **Брузерна сумісність** - WebRTC/Web Audio API потрібна свіжа версія браузера

## 📝 Ліцензія

MIT

## 👨‍💻 Розробка

Для розширення функціоналу:

1. Додати Voice Commands
2. Додати запис аудіо
3. Додати AI аналіз тону голосу
4. Додати статистику та логи
5. Додати темну/світлу тему

---

**Автор**: VoiceSignal Development Team  
**Мова**: Українська  
**Версія**: 1.0.0

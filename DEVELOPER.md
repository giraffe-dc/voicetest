# Voice Signal - Developer Guide

## 🎯 Проект завершен!

Успешно создан полнофункциональный сайт для визначення рівня голосу з синхронізацією виводу даних на різних пристроях.

## 📋 Хорошо, давайте заведемся!

### 1. Встановлення залежностей

```bash
npm install
```

### 2. Запуск локального development сервера

```bash
npm run dev
```

Сайт буде доступний на http://localhost:3000

### 3. Тестування на іншому пристрої

Для того, щоб тестувати на телефоні:

1. Дізнайтеся вашу IP адресу:
   ```bash
   # Windows
   ipconfig
   
   # Mac/Linux
   ifconfig
   ```

2. На телефоні відкрийте: `http://<ВАШ_IP>:3000`

## 🔍 Огляд файлів проекту

### Основні компоненти

```
app/
├── components/
│   ├── Equalizer.tsx              # 32-смуговий еквалайзер з кольорами
│   ├── RoomMode.tsx               # Режим комнаты для групи людей
│   └── SocketInitializer.tsx      # Ініціалізація WebSocket
│
├── hooks/
│   ├── useAudioAnalyzer.ts        # Обробка аудіо з мікрофону
│   ├── useSocketAudio.ts          # WebSocket комунікація (базовий режим)
│   └── useRoomSocket.ts           # WebSocket комунікація (режим кімнати)
│
├── page.tsx                       # Основна сторінка (базовий режим)
├── room/
│   └── page.tsx                   # Сторінка режима кімнати
│
└── globals.css                    # Глобальні стилі
```

### Backend

```
pages/
└── api/
    └── socket.ts                  # Socket.IO сервер (обробка WebSocket)
```

## 🎨 Особливості дизайну

### Еквалайзер

- **32 смуги** в реальному часі
- **Кольорові градієнти**: червоний → помаранчевий → жовтий → зелений → синій → пурпурний
- **Кольорові кружечки** на вершинах смуг з ефектом пульсації
- **Гловий ефект** (glow) для більш яскравого вигляду

### Адаптивна верстка

**На мобільних пристроях (< 768px)**:
- Вертикальний еквалайзер
- Компактні кнопки
- Оптимізовано для дотикового керування

**На комп'ютерах (≥ 768px)**:
- Горизонтальний еквалайзер
- Розширений інтерфейс
- Додаткова інформація

### Темна тема

- Головний фон: Темна фіолетова гама
- Акценти: Пурпурний, рожевий, блакитний
- Анімовані blob-и в фоні для динамічності

## 🔗 Режими роботи

### Режим 1: Базовий (/)

- **Мета**: Один користувач запускає мікрофон
- **Синхронізація**: Дані надсилаються на всі підключені пристрої
- **Вигляд**:
  - Великий еквалайзер в центрі
  - Кнопки керування
  - Статус підключення

### Режим 2: Кімната (/room)

- **Мета**: Декілька користувачів у одній "кімнаті"
- **Синхронізація**: Дані синхронізуються тільки в межах кімнати
- **Вигляд**:
  - Форма для введення ID кімнати
  - Список підключених користувачів
  - Переключення між аудіо разних користувачів

## 📊 Технічні деталі

### Web Audio API

```typescript
// Аналізатор частот
const analyzer = audioContext.createAnalyser();
analyzer.fftSize = 2048; // 1024 даних про частоти
```

### Socket.IO подій

**Клієнт → Сервер**:
- `audio-data`: Відправка частотних даних
- `join-room`: Підключення до кімнати

**Сервер → Клієнти**:
- `audio-update`: Оновлення частотних даних від інших користувачів
- `room-users-update`: Оновлення списку користувачів в кімнаті

## 🛠️ Розширення функціоналу

### Додати свої кольори

Відредагуйте `app/components/Equalizer.tsx`:

```typescript
const colors = [
  'from-red-500 to-orange-500',
  'from-orange-500 to-yellow-500',
  // Додайте більше кольорів тут
];
```

### Змінити кількість смуг

```typescript
// В Equalizer.tsx
const barCount = 64; // Змініть з 32 на інше значення
```

### Додати запис аудіо

```typescript
// В useAudioAnalyzer.ts
const recorder = new MediaRecorder(stream);
recorder.start();
// ... логіка запису
```

## 🚀 Деплой

### Vercel (рекомендується)

1. Завантажте проект на GitHub
2. Підключите GitHub до Vercel
3. Vercel автоматично розгорне

```bash
# або вручну
npm install -g vercel
vercel
```

### Docker

```dockerfile
FROM node:18-alpine
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build
EXPOSE 3000
CMD ["npm", "start"]
```

### ⚠️ Важно для продакшену

1. **HTTPS** - мікрофон потребує HTTPS на продакшене
2. **CORS** - налаштуйте CORS для вашого домену
3. **Сертифікат SSL** - отримайте від Let's Encrypt або подібної служби

## 🐛 Відомі проблеми та рішення

### Проблема: Мікрофон не працює на мобільному

**Рішення**: Переконайтесь:
- Сайт завантажується по HTTPS
- Браузер має дозвіл на доступ до мікрофона
- Мікрофон не заблокований в налаштуваннях браузера

### Проблема: Дані не синхронізуються

**Рішення**:
- Перевірте з'єднання з інтернетом
- Переванажте сторінку (F5)
- Перевірте консоль браузера на помилки (F12)

### Проблема: Низька якість звука на аналіз

**Рішення**: Змініть FFT size в `useAudioAnalyzer.ts`:
```typescript
analyzer.fftSize = 4096; // Вища точність (повільніше)
analyzer.fftSize = 1024; // Нижча точність (швидше)
```

## 📝 Ліцензія

MIT - Ви можете використовувати цей код для будь-яких цілей

## 🤝 Зворотний зв'язок

Якщо виявили проблему або маєте ідею для покращення, скажіть про це!

---

**Розроблено**: Voice Signal Team  
**Останнє оновлення**: 1 грудня 2025 р.  
**Версія**: 1.0.0
